# Google Gemini API key for the portfolio chat widget.
# Get one at https://aistudio.google.com/apikey
# IMPORTANT: this key ships in the client JS bundle (static site, no backend) —
# restrict it to this site's origin in Google AI Studio / Cloud Console
# (HTTP referrers: https://dipakmandlik.github.io/*) before deploying.
VITE_GEMINI_API_KEY=your-key-here
